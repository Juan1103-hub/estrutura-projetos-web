# Lições Aprendidas — Workspace

> Este arquivo é renderizado automaticamente pelo motor onp-spec.
> Não edite manualmente — use `onp-spec licoes add` para registrar lições.

Nenhuma lição registrada ainda.
